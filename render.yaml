services:
  - type: static
    name: fashnix-collections
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist/fashion-store
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
